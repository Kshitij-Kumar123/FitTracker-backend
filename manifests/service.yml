apiVersion: v1
kind: Service
metadata:
  name: app1-winter-cluster-ip-service
  namespace: prod
  labels:
    app: app1-winter
spec:
  type: ClusterIP
  selector:
    app: app1-winter
  ports:
    - port: 70
      targetPort: 70

